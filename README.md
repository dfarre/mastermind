# Mastermind game REST API

To run all tests, build the Docker environment,
```
$ docker-compose up
```
and then run
```
$ docker-compose exec api tox
```
The BDD test suite has ben created with [bdd-coder](https://bitbucket.org/coleopter/bdd-coder).
